import { TagsAndAccounts } from '../types'

const Portra400: TagsAndAccounts = {
    tags: [
        "portra400"
    ],
    accounts: [
    ]
};

const FilmType
    = { Portra400 };

export default FilmType
    ;